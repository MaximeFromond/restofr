{% include 'header.twig' %}


<div>
    <section class="blog">
        <h2>{{ post.title }}</h2>
        <div class="plats">
            {% for post in plats %}


                    <div>
                       <div>
                            <h3>{{ post.title }}</h3>
                            <p>{{ post.content }}</p>
                        </div>
                        <p>{{post.get_field('prix')}} €</p>
                    </div>


            {% endfor %}
        </div>
    </section>
</div>

